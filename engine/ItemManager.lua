
local ItemManager = {}

local object_list = {

}

function ItemManager.GetInstanceOfObject(object_model)

    if object_list[object_model] then
        return object_list[object_model]
    else
        local object_type = 

    end
end

function ItemManager.CreateObject(object_type)

end

function ItemManager.Initialize()

end

return ItemManager